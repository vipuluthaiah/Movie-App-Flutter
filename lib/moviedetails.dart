import 'package:flutter/material.dart';
// import 'json.dart';
// import 'network.dart';

class MovieListViewDetails extends StatelessWidget {
  final String movieName;
  final Future data;


  const MovieListViewDetails({Key key, this.movieName, this.data}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}